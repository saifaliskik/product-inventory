apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "product-inv-chart.fullname" . }}-test-connection"
  labels:
    {{- include "product-inv-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "product-inv-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
